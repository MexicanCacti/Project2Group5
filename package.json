{
  "name": "TellTales",
  "version": "1.0.0",
  "description": "",
  "main": "server/src/server.js",
  "scripts": {
    "start": "node server/src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "docker compose up --build",
    "down": "docker compose down -v --remove-orphans",
    "rebuild": "docker compose down -v --remove-orphans && docker compose up --build --force-recreate",
    "gcp-build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@google-cloud/vision": "^5.3.5",
    "@google/genai": "^1.50.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "crypto": "^1.0.1",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "firebase": "^12.12.1",
    "firebase-admin": "^13.8.0",
    "googleapis": "^171.4.0",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "private": true
}
